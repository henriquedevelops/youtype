import { MessagesFeedProps } from "@/src/typescriptTypes/message";
import { Box } from "@chakra-ui/react";
import { FunctionComponent as FC } from "react";

export const MessagesFeed: FC<MessagesFeedProps> = () => {
  return <Box>Feed</Box>;
};
